<div class="sc-block sc-sep sc-sep--{{ properties.spacing }}" data-sc-theme="{{ properties.theme }}">
    <div class="sc-container">
        <div class="sc-sep__row">
            <span class="sc-sep__line"></span>
            {{#if properties.label}}<span class="sc-sep__label">{{ properties.label }}</span><span class="sc-sep__line"></span>{{/if}}
        </div>
    </div>
</div>
